namespace GameManager.Sound {
    [System.Serializable]
    public enum SoundType {
        BLOB_BOUNCE_PADDLE,
        BLOB_BOUNCE_WALL,
        SHIELD_BREAK,
        GEM_COLLECT,
        BUTTON_PRESS,
        PURCHASE,
        FREEZE_POWER
    }
}
